# Servidor web
fastapi==0.115.12
uvicorn[standard]==0.34.0

# Validación y serialización
pydantic==2.11.1
pydantic-settings==2.8.1

# HTTP / clientes async
httpx==0.28.1

# SOAP
zeep==4.3.1

# XML y firma
lxml==5.3.2
# python-xmlsec requiere libxmlsec1: instalar via wheel precompilado en Windows
# pip install xmlsec  (wheel disponible en https://github.com/mehcode/python-xmlsec)
xmlsec==1.3.16

# Criptografía / certificados
cryptography==44.0.3
pyOpenSSL==25.0.0

# Plantillas
jinja2==3.1.6

# QR
qrcode[pil]==8.1
pillow==11.2.1

# Variables de entorno
python-dotenv==1.1.0

# Base de datos
sqlalchemy==2.0.40
alembic==1.15.2
psycopg2-binary==2.9.10        # driver PostgreSQL (usar psycopg[binary] para async)

# Redis (fase 2: colas de tickets y caché de sesiones)
redis==5.2.1

# Testing
pytest==8.3.5
pytest-asyncio==0.26.0
httpx==0.28.1

# Logging
python-json-logger==3.3.0
